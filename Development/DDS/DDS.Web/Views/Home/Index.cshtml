@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>RECETAS</h1>
    <p class="lead">El sistema responde a qué comer.</p>
    <p><a href="@Url.Action("Login", "Account")" class="btn btn-primary btn-lg">Iniciar Sesión</a></p>
</div>